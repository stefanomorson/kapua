services:
  db:
    container_name: db
    image: eclipsekapua/kapua-sql:${IMAGE_VERSION}
    ports:
      - "3306:3306"
  es:
    container_name: es
    image: docker.elastic.co/elasticsearch/elasticsearch:7.8.1
    ports:
      - "9200:9200"
      - "9300:9300"
    environment:
      - cluster.name=kapua-datastore
      - discovery.type=single-node
      - transport.host=_site_
      - transport.ping_schedule=-1
      - transport.tcp.connect_timeout=30s
  events-broker:
    container_name: events-broker
    image: eclipsekapua/kapua-events-broker:${IMAGE_VERSION}
    ports:
      - "5672:5672"
  message-broker:
    image: eclipsekapua/kapua-broker-artemis:${IMAGE_VERSION}
    container_name: message-broker
    expose:
      - 1893
    ports:
      - "1883:1883"
      - "1893:1893"
      - "5682:5672"
      - "61616:61616"
    depends_on:
      - db
      - events-broker
    environment:
      - CRYPTO_SECRET_KEY
      - KAPUA_DISABLE_DATASTORE
      - LOGBACK_LOG_LEVEL
      - DB_TARGET
      - DB_HOST
      - DB_NAME
      - DB_PORT
      - DB_SCHEMA_NAME
      - DB_USERNAME
      - DB_PASSWORD
      - DB_CONNECTION_ADDITIONAL_OPTIONS
      - DB_CONNECTION_SCHEME
      - DB_RESOLVER
      - DB_DRIVER
  service-authentication:
    image: eclipsekapua/kapua-service-authentication:${IMAGE_VERSION}
    container_name: service-auth
    ports:
      - 8100:8080
    depends_on:
      - db
      - events-broker
    environment:
      - LOGBACK_LOG_LEVEL
      - DB_TARGET
      - DB_HOST
      - DB_NAME
      - DB_PORT
      - DB_SCHEMA_NAME
      - DB_USERNAME
      - DB_PASSWORD
      - DB_CONNECTION_ADDITIONAL_OPTIONS
      - DB_CONNECTION_SCHEME
      - DB_RESOLVER
      - DB_DRIVER
  consumer-telemetry:
    container_name: consumer-telemetry
    image: eclipsekapua/kapua-consumer-telemetry:${IMAGE_VERSION}
    ports:
      - "8090:8080"
    depends_on:
      - db
      - es
      - events-broker
      - message-broker
    environment:
      - BROKER_URL
      - CRYPTO_SECRET_KEY
      - LOGBACK_LOG_LEVEL
      - DB_TARGET
      - DB_HOST
      - DB_NAME
      - DB_PORT
      - DB_SCHEMA_NAME
      - DB_USERNAME
      - DB_PASSWORD
      - DB_CONNECTION_ADDITIONAL_OPTIONS
      - DB_CONNECTION_SCHEME
      - DB_RESOLVER
      - DB_DRIVER
  consumer-lifecycle:
    container_name: consumer-lifecycle
    image: eclipsekapua/kapua-consumer-lifecycle:${IMAGE_VERSION}
    ports:
      - "8091:8080"
    depends_on:
      - db
      - events-broker
      - message-broker
    environment:
      - BROKER_URL
      - CRYPTO_SECRET_KEY
      - LOGBACK_LOG_LEVEL
      - DB_TARGET
      - DB_HOST
      - DB_NAME
      - DB_PORT
      - DB_SCHEMA_NAME
      - DB_USERNAME
      - DB_PASSWORD
      - DB_CONNECTION_ADDITIONAL_OPTIONS
      - DB_CONNECTION_SCHEME
      - DB_RESOLVER
      - DB_DRIVER
  kapua-console:
    container_name: kapua-console
    image: eclipsekapua/kapua-console:${IMAGE_VERSION}
    ports:
      - "8080:8080"
    depends_on:
      - db
      - es
      - events-broker
      - message-broker
    environment:
      - CRYPTO_SECRET_KEY
      - KAPUA_DISABLE_DATASTORE
      - LOGBACK_LOG_LEVEL
      - DB_TARGET
      - DB_HOST
      - DB_NAME
      - DB_PORT
      - DB_SCHEMA_NAME
      - DB_USERNAME
      - DB_PASSWORD
      - DB_CONNECTION_ADDITIONAL_OPTIONS
      - DB_CONNECTION_SCHEME
      - DB_RESOLVER
      - DB_DRIVER
  kapua-api:
    container_name: kapua-api
    image: eclipsekapua/kapua-api:${IMAGE_VERSION}
    ports:
      - "8081:8080"
      - "8444:8443"
    depends_on:
      - db
      - es
      - events-broker
      - message-broker
    environment:
      - API_CORS_ORIGINS_ALLOWED
      - CRYPTO_SECRET_KEY
      - KAPUA_DISABLE_DATASTORE
      - LOGBACK_LOG_LEVEL
      - SWAGGER=${KAPUA_SWAGGER_ENABLE:-true}
      - AUTH_TOKEN_TTL=${AUTH_TOKEN_TTL:-1800000}
      - REFRESH_AUTH_TOKEN_TTL=${REFRESH_AUTH_TOKEN_TTL:-18000000}
      - CORS_ENDPOINTS_REFRESH_INTERVAL=${CORS_ENDPOINTS_REFRESH_INTERVAL:-60}
      - COMMONS_VERSION
      - COMMONS_BUILD_NUMBER
      - DB_TARGET
      - DB_HOST
      - DB_NAME
      - DB_PORT
      - DB_SCHEMA_NAME
      - DB_USERNAME
      - DB_PASSWORD
      - DB_CONNECTION_ADDITIONAL_OPTIONS
      - DB_CONNECTION_SCHEME
      - DB_RESOLVER
      - DB_DRIVER
  job-engine:
    container_name: job-engine
    image: eclipsekapua/kapua-job-engine:${IMAGE_VERSION}
    expose:
      - 8080
    depends_on:
      - db
      - events-broker
      - message-broker
    environment:
      - CRYPTO_SECRET_KEY
      - KAPUA_DISABLE_DATASTORE
      - LOGBACK_LOG_LEVEL
      - DB_TARGET
      - DB_HOST
      - DB_NAME
      - DB_PORT
      - DB_SCHEMA_NAME
      - DB_USERNAME
      - DB_PASSWORD
      - DB_CONNECTION_ADDITIONAL_OPTIONS
      - DB_CONNECTION_SCHEME
      - DB_RESOLVER
      - DB_DRIVER
